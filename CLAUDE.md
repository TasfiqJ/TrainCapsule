# TrainCapsule factory routing

Read only the task packet and the exact protected source sections it cites. Do not load the full master plan for routine tasks.

The active product loops are Close, Qualify, and Exchange. The initial packs are `PRE_COLLECTIVE_LIFECYCLE_CONTRACT_V1` and `CHECKPOINT_RESUME_STATE_CONSISTENCY_V1`.

Never modify `docs/source-of-truth/final-2026-08-09/`, expected evidence, private gates, release authority, or another role's handoff. Never create an API key or paid-credit fallback.

Failed, unknown, skipped, unattributed, invalid-oracle, infrastructure-error, and external-validation-required states are not passes. Stop on a source contradiction, protected-path change, missing oracle, security boundary failure, or unverified release route.

Persist bounded handoffs under `.factory/handoffs/`. Product work stays disabled until the installer records successful calibration and activation.
