---
name: run-traincapsule
description: Run the TrainCapsule application and verify real behavior rather than relying only on unit tests.
---

# Run and verify

Use the repository's documented commands to start the real Close, Qualify, Exchange, CLI, API, viewer, backend, or pack path applicable to the task. Exercise one clean control, one failure path, and one boundary case. Capture raw command output, source locks, truth states, and artifact paths. Do not claim a workflow, backend, pack, or UI works from static inspection alone.
