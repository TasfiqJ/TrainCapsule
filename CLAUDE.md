# TrainCapsule factory routing

Read only the task packet and the exact protected V3.1-ZH source sections it cites. Do not load a historical source generation or full master plan for routine tasks. Reject a packet whose active-generation, source-manifest, or context digest does not resolve exactly.

The active product loops are Close, Qualify, and Exchange. The initial packs are `PRE_COLLECTIVE_LIFECYCLE_CONTRACT_V1` and `CHECKPOINT_RESUME_STATE_CONSISTENCY_V1`.

Never modify any historical source bundle, the active normative source from a candidate task, expected evidence, verifier policy, release or activation authority, or another role's handoff. Never create an API key or paid-credit fallback.

Failed, unknown, skipped, unattributed, invalid-oracle, infrastructure-error, and external-validation-required states are not passes. Stop on a mixed or contradictory source generation, protected-path change, missing independent oracle, security boundary failure, or unverified pull-request route. Missing external facts remain scoped `WAITING_EXTERNAL`; missing machine authority is `BLOCKED_POLICY`.

Persist bounded handoffs under `.factory/handoffs/`. No agent session may self-certify trust, merge, activation, customer evidence, or commercial maturity. Product work stays disabled until exact-SHA checks and a signed off-repository activation receipt authorize the zero-founder-intervention controller.
