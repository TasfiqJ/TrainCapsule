# TrainCapsule V3.1-ZH remaining acceptance

This is the durable no-forgetting checklist for the active V3.1-ZH remediation. It is
bound to implementation candidate `9b95cfb766ad6c3eb3cfb6f262d5a6625d6482ad` and tree
`cc498044793cc865b06e1375a5442f75ef6fada5`. It is a progress record, not an
activation receipt.

## Safety state

- Authoritative stop marker: `factory/state/STOP` is present.
- `factory/state/PAUSE` and `factory/state/HARD_STUCK` are absent.
- Automatic Activation, Calibration Recovery, and Lights-Out scheduled tasks are disabled.
- The GitHub runner listener is the only active TrainCapsule task process.
- The independent verifier, installed runtime, GitHub App authority, live receipts, and
  post-activation observer are not installed or accepted.
- All eight exact-candidate hosted workflows and GitGuardian pass, but the GitHub ruleset
  API returns no rulesets. Repository auto-merge is enabled but is not release authority
  without the exact required ruleset and Machine-policy App.
- WSL systemd is disabled; no verifier/runtime unit can be installed and observed until the
  reviewed host bootstrap enables it and restarts WSL.
- Activation remains `STOPPED / NOT AUTHORIZED`.

## Candidate classification

The Phase-0 classifications remain immutable historical facts. The current candidate
evaluation is intentionally non-final:

| Candidate state | Rows | Meaning |
|---|---:|---|
| Local implementation pending integrated acceptance | 100 | Focused implementation evidence exists, but complete local, hosted, installed, and applicable live proof is not yet attached. |
| External fact pending | 14 | Attributable customer, market, competitor, GPU, paid-use, or other outside evidence is absent. |
| Independent provisioning pending | 10 | Root-owned verifier/service installation, GitHub App identity, ruleset, private gate, or signed receipt authority is absent. |
| Live canary pending | 32 | Exact-installed-candidate 20-canary and/or seven-event observer proof is absent. |
| M0 external evidence pending | 2 | Independent source authorization and real automated-PR/CI/merged-main receipts are absent. |

The exact row membership is machine-bound in
`docs/migrations/V3_1_ZH_158_ROW_FINDINGS_LEDGER.json` and regenerated by
`scripts/update_v31_findings_candidate.py`.

## Required sequence

1. Freeze and push the implementation candidate without evidence-document circularity.
2. Regenerate the migration inventory and candidate findings ledger.
3. Run every source, package, roadmap, policy, schema, static, unit, hostile, packaging,
   clean-checkout, and pre-evidence quality gate against the exact candidate.
4. Push the evidence-document commit and require every hosted PR check at that exact head.
5. Keep every hosted check green without weakening policy or tests; repeat after each
   evidence-only or configuration-binding commit.
6. Build and dry-run the privileged deployment bundle; verify every file, owner, mode,
   executable, runtime, wheel, import origin, service, rollback journal, and access boundary.
7. Provision the independent verifier authority, revocation state, oracle, GitHub App,
   exact main ruleset, check publisher, anchor updater, deployment refresher, and activation
   services. Never expose private authority to the controller.
8. Run all 20 mandatory live canaries against the exact installed candidate. Missing or
   unavailable live evidence remains `BLOCKED`; it must never be simulated as `PASS`.
9. Issue and verify the exact signed LIVE activation receipt, perform the journaled STOP
   transition, start the pinned controller, and require the ordered seven-event observer.
10. Merge only the exact verified SHA through the automated PR-only path. Record final
    external receipts and update the ledger without rewriting historical Phase-0 facts.

## Non-negotiable proof rules

- Code presence, a test filename, prose, a prior SHA, and a self-authored JSON object are not closure.
- Every final row needs exact candidate SHA/tree, artifact path and digest, positive control,
  hostile control, rollback behavior, and any required external signed authority.
- No direct push to `main`, force push, human approval, PAT authority, owner override,
  candidate self-certification, or fabricated external evidence is permitted.
- The controller must remain stopped until every applicable critical/high acceptance row,
  server rule, receipt, live canary, and observation passes.
