# Supersession and Migration Notice

- **Date:** 9 August 2026
- **Status:** Final

This bundle is the only active TrainCapsule product authority.

## Replace

Replace earlier:

- executive decisions;
- product definitions;
- technical architectures;
- trust/capsule specifications;
- acquisition and career theses;
- roadmaps;
- Claude build prompts;
- setup/resume/audit prompts;
- all-in-one master plans.

## Preserve only as historical input

Earlier materials may be retained outside the live repository for provenance. They must not:

- appear in the active authority order;
- generate tasks;
- override schemas or incident packs;
- restore an incident-only business;
- require cross-company acceptance for initial value;
- use globally minimal reproducer language;
- treat first observed divergence as root cause;
- or reactivate broad first-release incident classes.

## Final active product

```text
Close
  failure → experiment → recovery assurance → incident contract

Qualify
  incident/critical-workload contract → stack change → bounded decision

Exchange
  customer/provider-local evidence → native/federated technical artifact
```

## Migration verification

The live repository passes migration only when:

- no superseded product prompt is active;
- source authority matches the final read order;
- final hashes match the manifest;
- first task is T001 from the final 124-task ledger;
- initial packs are exactly the flagship lifecycle pack and checkpoint/resume pack;
- OpGuard/PrismLLM are treated as backend baselines;
- external facts remain `EXTERNAL_VALIDATION_REQUIRED`;
- build-order prohibitions remain active.
