# TrainCapsule V3 source of truth

This directory contains the active V3 authority for the bounded TrainCapsule migration and initial product build authorized on 11 August 2026.

The migration base is:

```text
6b480232fa92b069103da44c475bd17bcb3e6bd1
```

The prior bundle at `docs/source-of-truth/final-2026-08-09/` remains immutable historical evidence. It is not rewritten or treated as active authority.

Normative policy and current factual authority are intentionally separate. `SOURCE_PRECEDENCE.md` defines the exact order, conflict behavior, owner-directed machine-policy scope, and stale-fact process. `13_SOURCE_REGISTER_V3.md` is current factual authority; changes in external facts may trigger review but cannot silently rewrite normative policy.

Installing this bundle authorizes repository migration and controlled engineering work. It does not establish customer demand, payment, adoption, native advantage, production readiness, commercial support, or approval for external use. The repository owner's later zero-human directive replaces qualified-human approval with candidate-bound deterministic machine-policy receipts; it does not replace required attributable external evidence. External and commercial releases therefore remain blocked while that evidence is absent, invalid, stale, or outside the supported envelope.

`FINAL_MANIFEST_V3.json` is generated by `scripts/generate_v3_manifest.py`. It hashes canonical UTF-8 LF text with exactly one trailing newline and deliberately excludes its own hash.
