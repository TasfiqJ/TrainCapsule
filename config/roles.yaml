planner:
  prompt_file: prompts/autonomous_planner.md
  model: sonnet
  fallback_models: [opus]
  effort: high
  max_turns: 200
  max_budget_usd: 1000.0
  task_budget_tokens: null
  tools: [Read, Grep, Glob, Write, Edit, Bash, WebFetch, WebSearch, Agent]
  disallowed_tools: []
  permission_mode: acceptEdits
  read_only: false

specification:
  prompt_file: prompts/specification.md
  model: opus
  fallback_models: [sonnet]
  effort: xhigh
  max_turns: 200
  max_budget_usd: 1000.0
  task_budget_tokens: null
  tools: [Read, Grep, Glob, Write, Edit, Bash, WebFetch, WebSearch, Agent]
  disallowed_tools: []
  permission_mode: acceptEdits
  read_only: false

research:
  prompt_file: prompts/research.md
  model: opus
  fallback_models: [sonnet]
  effort: xhigh
  max_turns: 200
  max_budget_usd: 1000.0
  task_budget_tokens: null
  tools: [Read, Grep, Glob, Write, Edit, Bash, WebFetch, WebSearch, Agent]
  disallowed_tools: []
  permission_mode: acceptEdits
  read_only: false

builder:
  prompt_file: prompts/builder.md
  model: sonnet
  fallback_models: [opus]
  effort: max
  max_turns: 200
  max_budget_usd: 1000.0
  task_budget_tokens: null
  tools: [Read, Grep, Glob, Write, Edit, Bash, WebFetch, WebSearch, Agent]
  disallowed_tools: []
  permission_mode: acceptEdits
  read_only: false

integration_scout:
  prompt_file: prompts/integration_scout.md
  model: sonnet
  fallback_models: [opus]
  effort: high
  max_turns: 100
  max_budget_usd: 1000.0
  task_budget_tokens: null
  tools: [Read, Grep, Glob, Bash, ListAgents, SendMessage]
  disallowed_tools: [Write, Edit, Agent]
  permission_mode: dontAsk
  read_only: true
  peer_messaging: true
  session_name_template: tc-{task_id}-integration-scout-{attempt}

adversary:
  prompt_file: prompts/adversary.md
  model: opus
  fallback_models: [sonnet]
  effort: max
  max_turns: 100
  max_budget_usd: 1000.0
  task_budget_tokens: null
  tools: [Read, Grep, Glob, Bash, WebFetch, WebSearch]
  disallowed_tools: [Write, Edit, Agent]
  permission_mode: dontAsk
  read_only: true

audit:
  prompt_file: prompts/audit.md
  model: sonnet
  fallback_models: [opus]
  effort: xhigh
  max_turns: 80
  max_budget_usd: 1000.0
  task_budget_tokens: null
  tools: [Read, Grep, Glob, Bash]
  disallowed_tools: [Write, Edit, Agent]
  permission_mode: dontAsk
  read_only: true

security:
  prompt_file: prompts/security.md
  model: opus
  fallback_models: [sonnet]
  effort: max
  max_turns: 120
  max_budget_usd: 1000.0
  task_budget_tokens: null
  tools: [Read, Grep, Glob, Bash, WebFetch, WebSearch]
  disallowed_tools: [Write, Edit, Agent]
  permission_mode: dontAsk
  read_only: true

performance:
  prompt_file: prompts/performance.md
  model: sonnet
  fallback_models: [opus]
  effort: xhigh
  max_turns: 100
  max_budget_usd: 1000.0
  task_budget_tokens: null
  tools: [Read, Grep, Glob, Bash]
  disallowed_tools: [Write, Edit, Agent]
  permission_mode: dontAsk
  read_only: true

value_validator:
  prompt_file: prompts/value_validator.md
  model: sonnet
  fallback_models: [opus]
  effort: xhigh
  max_turns: 100
  max_budget_usd: 1000.0
  task_budget_tokens: null
  tools: [Read, Grep, Glob, Bash, WebFetch, WebSearch]
  disallowed_tools: [Write, Edit, Agent]
  permission_mode: dontAsk
  read_only: true

value_adversary:
  prompt_file: prompts/value_adversary.md
  model: opus
  fallback_models: [sonnet]
  effort: max
  max_turns: 100
  max_budget_usd: 1000.0
  task_budget_tokens: null
  tools: [Read, Grep, Glob, Bash, WebFetch, WebSearch]
  disallowed_tools: [Write, Edit, Agent]
  permission_mode: dontAsk
  read_only: true

release:
  prompt_file: prompts/release.md
  model: sonnet
  fallback_models: [opus]
  effort: xhigh
  max_turns: 100
  max_budget_usd: 1000.0
  task_budget_tokens: null
  tools: [Read, Grep, Glob, Bash]
  disallowed_tools: [Write, Edit, Agent]
  permission_mode: dontAsk
  read_only: true

recovery:
  prompt_file: prompts/recovery.md
  model: opus
  fallback_models: [sonnet]
  effort: max
  max_turns: 200
  max_budget_usd: 1000.0
  task_budget_tokens: null
  tools: [Read, Grep, Glob, Write, Edit, Bash, WebFetch, WebSearch, Agent]
  disallowed_tools: []
  permission_mode: acceptEdits
  read_only: false

factory_repair:
  prompt_file: prompts/factory_repair.md
  model: opus
  fallback_models: [sonnet]
  effort: max
  max_turns: 200
  max_budget_usd: 1000.0
  task_budget_tokens: null
  tools: [Read, Grep, Glob, Write, Edit, Bash, WebFetch, WebSearch, Agent]
  disallowed_tools: []
  permission_mode: acceptEdits
  read_only: false
